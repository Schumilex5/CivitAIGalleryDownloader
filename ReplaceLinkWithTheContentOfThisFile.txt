javascript:(async()=>{await new Promise(r=>setTimeout(r,1500));const g=Array.from(document.querySelectorAll('.mantine-Grid-root')).find(g=>g.querySelector('.mantine-Grid-col img[src*="image.civitai.com"]'));if(!g)return;const imgs=Array.from(g.querySelectorAll('.mantine-Grid-col img[src*="image.civitai.com"]')).filter(i=>!i.closest('.mantine-Card-root')&&!i.closest('[class*="CreatorCard_"]')&&i.getBoundingClientRect().width>0&&i.getBoundingClientRect().height>0);if(!imgs.length)return;const urls=imgs.map(i=>i.src.replace(/\/anim=.*?\/|,optimized=true|,width=\d+/g,'/').split('?')[0]).filter(u=>u&&u.startsWith('https'));for(let i=0;i<urls.length;i++){try{const r=await fetch(urls[i]);const b=await r.blob();const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=`civitai_image_${i+1}.jpg`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(a.href);}catch{}}})();
