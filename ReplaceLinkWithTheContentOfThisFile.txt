javascript:(async()=>{const imgs=Array.from(document.querySelectorAll('.mantine-Grid-col img[src*="image.civitai.com"]')).filter(img=>!img.closest('.mantine-Card-root')&&!img.closest('[class*="CreatorCard_"]'));if(!imgs.length)return;const urls=imgs.map(img=>img.src.replace(/\/anim=.*?\/|,optimized=true|,width=\d+/g,'/').split('?')[0]).filter(u=>u&&u.startsWith('https'));for(let i=0;i<urls.length;i++){try{const res=await fetch(urls[i]);const blob=await res.blob();const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`civitai_image_${i+1}.jpg`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(a.href);}catch{}}})();
